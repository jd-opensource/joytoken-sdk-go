package joytoken

// Anthropic adapter and RunMessage loop coverage lives in
// client_compat_adapter_test.go so the Chat/Responses/Anthropic ownership rules
// are exercised against the same capture transport.
